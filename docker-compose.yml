services:
  postgres:
    image: postgres:17
    restart: unless-stopped
    network_mode: "bridge"
    environment:
      POSTGRES_DB: nitro_repo
      POSTGRES_USER: nitro
      POSTGRES_PASSWORD: nitro
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nitro -d nitro_repo"]
      interval: 5s
      timeout: 5s
      retries: 10

  nitro_repo:
    build: .
    restart: unless-stopped
    network_mode: "bridge"
    depends_on:
      postgres:
        condition: service_healthy
    links:
      - postgres
    command: ["start", "--config", "/data/nitro_repo.toml"]
    ports:
      - "8080:6742"
    labels:
      - "traefik.frontend.rule=Host:nitro.sudoers.dev"
      - "traefik.enable=true"
      - "traefik.port=6742"
    volumes:
      - nitro_repo_data:/data
      - ./docker/config.toml:/data/nitro_repo.toml:ro

volumes:
  nitro_repo_data:
  postgres_data:

networks:
  default:
    external: true
    name: bridge
