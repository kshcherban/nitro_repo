apiVersion: v1
kind: Service
metadata:
  name: {{ include "nitro-repo-chart.fullname" . }}
  labels:
    {{- include "nitro-repo-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nitro-repo-chart.selectorLabels" . | nindent 4 }}